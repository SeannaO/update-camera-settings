#!/bin/sh

n=$(pgrep node | wc -l)
if [ "$n" -eq 0 ] 
then 
  echo "you shall pass" 
  /opt/apps/solink/CameraManager/node /opt/apps/solink/CameraManager/app /nfs/solink
else
  echo `date` > /opt/apps/solink/forever_test
fi
